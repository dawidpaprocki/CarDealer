package com.example.car_dealer;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ApplicationConfiguration {
    // because we are using spring boot, we do not need anything in configuration class. Spring Boot is focusing on
    // auto configuration of used modules, we do not need to be bothered by that, as long as we are using parameters
    // and filesnames that are standard for spring
}
